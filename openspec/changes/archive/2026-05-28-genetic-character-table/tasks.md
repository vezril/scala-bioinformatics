## 1. GeneticCharacterTableProblem ADT (Red phase first)

- [x] 1.1 Write `src/test/scala/bio/domain/analysis/GeneticCharacterTableProblemSpec.scala` covering: accepts the canonical 5×8 Rosalind sample; accepts 100×300 at the upper boundary; rejects empty input as `EmptyInput`; rejects 101 strings as `TooManyStrings(101, 100)`; rejects a 301-char string as `StringTooLong(0, 301, 300)`; rejects mismatched lengths as `InconsistentLength(1, 8, 7)`; rejects a non-characterizable 3-symbol column as `NonCharacterizable(0, 3)`; `apply` and `copy` leak-proofness via `assertDoesNotCompile`. Use a `def dna(s: String): DnaString` helper that calls `DnaString.from(s).getOrElse(sys.error(...))`. Run `sbt Test/compile` and confirm Red.
- [x] 1.2 Create `src/main/scala/bio/domain/analysis/GeneticCharacterTableProblemError.scala` as a `sealed trait` with cases `EmptyInput`, `TooManyStrings(count: Int, max: Int)`, `StringTooLong(index: Int, length: Int, max: Int)`, `InconsistentLength(index: Int, expected: Int, actual: Int)`, `NonCharacterizable(columnIndex: Int, distinctCount: Int)`.
- [x] 1.3 Create `src/main/scala/bio/domain/analysis/GeneticCharacterTableProblem.scala` as `sealed abstract case class GeneticCharacterTableProblem(sequences: Vector[DnaString], size: Int, length: Int)` with companion `from(sequences: Vector[DnaString]): Either[GeneticCharacterTableProblemError, GeneticCharacterTableProblem]` performing the 5-step validation cascade in the documented order. The characterizability check is a column-by-column scan returning the first column whose distinct-symbol count exceeds 2.
- [x] 1.4 Run `sbt testOnly bio.domain.analysis.GeneticCharacterTableProblemSpec` and confirm Green.

## 2. GeneticCharacterTable algorithm (Red phase first)

- [x] 2.1 Write `src/test/scala/bio/algorithms/analysis/GeneticCharacterTableSpec.scala` covering: canonical Rosalind sample → `Vector("10110", "10100")`; three identical `ACGT` strings → `Vector.empty`; a matrix with constant columns except column 1 alternating `A C A C` (4 rows) → `Vector("1010")` (since row 0 is `A`, then C → 0, A → 1, C → 0); a 5-row matrix where every column is either uniform or a 4-1 split → `Vector.empty`; row-length invariant — every emitted row has length `problem.size`. Use a private `fixture(strings: String*): GeneticCharacterTableProblem` helper that maps each through `DnaString.from`, builds the `GeneticCharacterTableProblem`, and `getOrElse(sys.error(...))`. Run `sbt Test/compile` and confirm Red.
- [x] 2.2 Create `src/main/scala/bio/algorithms/analysis/GeneticCharacterTable.scala` exposing `def compute(problem: GeneticCharacterTableProblem): Vector[String]`. Algorithm: iterate `j` in `0 until problem.length`; capture `problem.sequences(0).value.charAt(j)` as the reference symbol; build a `Vector[Char]` of column-`j` chars by mapping each row; count matches to the reference; skip if the partition is trivial (matches = `size` or matches = `0` — i.e. one distinct symbol — or `matches < 2` or `size - matches < 2`); otherwise build a row via `column.map(c => if (c == reference) '1' else '0').mkString`. Result is the in-order list of emitted rows.
- [x] 2.3 Run `sbt testOnly bio.algorithms.analysis.GeneticCharacterTableSpec` and confirm Green.

## 3. Refactor & Final Verification

- [x] 3.1 Re-read both new source files and tests; tighten Scaladoc to (a) cite Rosalind problem 33 and the row-encoding convention "row i's bit is `'1'` iff its symbol matches row 0's at the same column", (b) cross-reference the sister spec 32 `CharacterTable` (different convention; called out explicitly). No stray imports.
- [x] 3.2 Run `sbt test` and confirm the full suite passes with zero regressions.
