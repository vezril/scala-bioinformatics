## 1. CharacterTableProblem ADT (Red phase first)

- [x] 1.1 Write `src/test/scala/bio/domain/graph/CharacterTableProblemSpec.scala` covering: accepts the parsed Rosalind sample `(dog,((elephant,mouse),robot),cat);` with `leafLabels == Vector("cat", "dog", "elephant", "mouse", "robot")`; accepts a single-leaf tree `a;`; accepts a 200-leaf flat tree; rejects a 201-leaf flat tree as `TooManyTaxa(201, 200)`; companion `apply` and `copy` leak-proofness via `assertDoesNotCompile`. Use a `def buildFlatTree(n: Int): NewickTree` helper that produces an N-children-of-root flat tree with labels `t001..tN`. Run `sbt Test/compile` and confirm Red.
- [x] 1.2 Create `src/main/scala/bio/domain/graph/CharacterTableProblemError.scala` as a `sealed trait` with case `TooManyTaxa(count: Int, max: Int)`.
- [x] 1.3 Create `src/main/scala/bio/domain/graph/CharacterTableProblem.scala` as `sealed abstract case class CharacterTableProblem(tree: NewickTree, leafLabels: Vector[String])` with companion `from(tree: NewickTree): Either[CharacterTableProblemError, CharacterTableProblem]`. The companion's private `collectLeaves(node)` helper recursively descends, collecting `label.get` when a node has no children and is labelled. `leafLabels` is the distinct-sorted result. Cap enforced at 200.
- [x] 1.4 Run `sbt testOnly bio.domain.graph.CharacterTableProblemSpec` and confirm Green.

## 2. CharacterTable algorithm (Red phase first)

- [x] 2.1 Write `src/test/scala/bio/algorithms/graph/CharacterTableSpec.scala` covering: canonical Rosalind sample → `Vector("00110", "00111")`; single-leaf `a;` → `Vector.empty`; flat `(a,b,c,d);` → `Vector.empty`; balanced quartet `((a,b),(c,d));` → `Vector("0011")`; deeper nesting `((a,b),((c,d),(e,f)));` → `Vector("000011", "001100", "001111")`; and a row-length invariant — every row in every non-empty result has length `problem.leafLabels.size`. Use a private `fixture(newick: String): CharacterTableProblem` helper that parses + builds the `CharacterTableProblem` and `getOrElse(sys.error(...))`. Run `sbt Test/compile` and confirm Red.
- [x] 2.2 Create `src/main/scala/bio/algorithms/graph/CharacterTable.scala` exposing `def compute(problem: CharacterTableProblem): Vector[String]`. Algorithm: (a) build a `Map[String, Int]` from each leaf label to its column index from `problem.leafLabels`; (b) define a recursive `collectSubtreeLeaves(node): Set[String]`; (c) define a recursive `enumerateSplits(node): Vector[Set[String]]` that walks every non-root descendant, recording each one's subtree-leaves; (d) for each subtree-set `s`, skip if `s.size < 2 || s.size > n - 2`; (e) for each retained split, produce the row using the lex-first-taxon-side-gets-`0` convention; (f) `distinct.sorted` the rows. Use `@tailrec` only where natural — the tree walks are straight recursion.
- [x] 2.3 Run `sbt testOnly bio.algorithms.graph.CharacterTableSpec` and confirm Green.

## 3. Refactor & Final Verification

- [x] 3.1 Re-read both new source files and tests; tighten Scaladoc, ensure the deduplication step is justified inline (different edges produce different splits in a tree — but the same split can be generated by both endpoints during enumeration, hence `.distinct`), and ensure naming matches the rest of the framework. No stray imports.
- [x] 3.2 Run `sbt test` and confirm the full suite passes with zero regressions.
